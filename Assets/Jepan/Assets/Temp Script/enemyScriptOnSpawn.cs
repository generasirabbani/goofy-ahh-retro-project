using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class enemyScriptOnSpawn : MonoBehaviour
{
    // Start is called before the first frame update
    AstarPath test;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
